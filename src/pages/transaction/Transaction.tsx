import Container from "../../components/container/Container";

const Transaction = () => {
  return <Container menuType="transaction">ini adalah transaction</Container>;
};

export default Transaction;
